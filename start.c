#include <stdio.h>

int main(int argc, char** argv) {
  puts("Hello, world!");
  int sum(int x, int y) {
    int result = x + y;
    return result;
  }
  int random = x + y; 
  int count = 10;
  return 0;
}